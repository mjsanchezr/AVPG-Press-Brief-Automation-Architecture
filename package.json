{
  "name": "avpg-press-brief-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install",
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "@google/genai": "^2.2.0"
  }
}
